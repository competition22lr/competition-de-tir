import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.competitiondetir.trepanier.cc',
  appName: 'competition-de-tir',
  webDir: 'dist\\competition-de-tir\\browser'
};

export default config;
